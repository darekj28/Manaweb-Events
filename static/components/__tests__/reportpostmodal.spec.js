import ReportPostModal from '../ReportPostModal.jsx';

describe('ReportPostModal', () => {
	it('should report when button is clicked', () => {
		expect(true).toEqual(true);
	});
	// it('renders correctly', () => {
	// 	const post = {unique_id : 1};
	// 	const rpm = renderer.create(
	// 		<ReportPostModal post={post}/>
	// 		);
	// 	expect(rpm).toMatchSnapshot();
	// });
})